/// مفتاح تبديل مركزي واحد بين البيانات الوهمية (Mock) وFirebase الحقيقي.
///
/// اتركه `false` حتى تُنهي خطوات ربط Firebase الموضحة في README.md (قسم
/// "الربط الفعلي بـ Firebase")، ثم غيّره إلى `true`. كل المزوّدات
/// (auth/catalog/orders) تقرأ هذه القيمة تلقائياً وتبدّل التنفيذ المناسب —
/// لا حاجة لتعديل أي شاشة.
const bool kUseFirebase = false;
